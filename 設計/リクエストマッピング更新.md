# リクエストマッピング

|メソッド|パラメーター|サーブレット|サービス|DAO|SQL|テーブル|内容|
| --- | --- | --- | --- | --- | --- | --- | --- |
POST/GET|action=login|MenuServlet||MenuDAO  getName(String staff_if)|SELECT staff_name FROM adminlist WHERE staff_id = ?| adminlist|SQLで'id'引数に当たる会員の名前を取得する
POST/GET|action=login |MenuServlet||MenuDAO getPass(String staff_id) |SELECT staff_password FROM adminlist WHERE Staff_id = ?|adminlist |全ての会員情報を取得する
POST/GET|action=confirm|MemberServlet||MemberDAO getIdName()|select member_id, member_name from memberlist order by member_id desc limit 1|memberlist|テーブル上、最後に登録された人のIDと名前を取得する
POST/GET|action=search|MemberServlet||MemberDAO findAll(int member_id)|Select * from memberlist where member_id = ?|memberlist|全ての会員情報を取得する
POST/GET|action=confirm|MemberServlet||MemberDAO addMember(String member_name, String member_address, String member_tel, String member_mail, String member_birth, String current_date)|insert into memberlist(member_name, member_address, member_tel, member_mail, member_birth, member_registdate) values(?, ?, ?, ?, ?, ?)|memberlist|書いてもらった情報をもとにMemberListテーブルに登録後、最終的に登録された会員のIDと名前を返す
POST/GET|action=update|MemberServlet||MemberDAO updateAll(int member_id, String member_name, String member_address, String member_tel, String member_mail, String member_birth)|update memberlist set member_name = ?, member_address = ?, member_tel = ?, member_mail = ?, member_birth = ? where member_id = ?|memberlist |既存のレコードの情報を引数に書いてあるどおり、変更する。
POST/GET|action=cancel|MemberServlet||MemberDAO updateCancelDate(int member_id, String current_date) |update memberlist set member_canceldate = ? where member_id = ?|memberlist |既存のレコードの退会年月日に情報を追加する
POST/GET|action=add1 |BookServlet||BookDAO addStockList(String isbn, date arrival_date)|insert into stocklist(isbn,arrival_date) values(?, ?)|stocklist|資料を在庫台帳に新規登録する
POST/GET|action=add2 |BookServlet||BookDAO addCatalogList(String isbn, String title, int category_code, String writer, String publisher, date publish_date) <br> BookDAO addStockList(String isbn, date arrival_date)|insert into cataloglist(isbn,title,category_code,writer,publisher,publish_date) values(?,?,?,?,?,?) <br> insert into stocklist(isbn,arrival_date) values(?, ?)|cataloglist|資料を在庫台帳に新規登録する
POST/GET|action=discardsearch |BookServlet||BookDAO getStockListByBookId(int book_id)|select s.book_id, s.isbn, c.title, s.arrival_date from stocklist s join cataloglist c on s.isbn = c.isbn where s.book_id = ?|stocklist/cataloglist |資料IDで資料情報を検索する
POST/GET|action=discard|BookServlet||BookDAO  updateStockListDiscard(int book_id, date discard_date,String remarks)|update stocklist set discard_date = ?, remarks = ? where book_id = ?|stocklist|紛失した資料を廃棄する日付を廃棄年月日として記録する
POST/GET|action=return|BookServlet||BookDAO updateRentDate(String member_id, String book_id, String current_date) |update rentlist set rent_date = ?, remarks = null where member_id = ? and book_id = ?|rentlist|会員が資料を返却したらテーブルにその日付を登録
POST/GET|action=rent |BookServlet||BookDAO addRentlistAndGetInfo(String member_id, String book_id, String return_deadline, String current_date)|insert into rentlist(member_id, book_id, rent_date, return_deadline) values(?, ?, ?, ?) select * from rentlist where member_id = ? and book_id = ? and rent_date = ?|rentlist|会員が資料を返却したらテーブルにその日付を登録
POST/GET|action=booksearch|BookServlet||BookDAO getIsbnByBookId(int book_id) <br> BookDAO getCatalogListInfo(String type, String value) <br> BookDAO getBookIdByIsbn(String isbn) <br> BookDAO  getBookIdByTitle(String title) |select isbn from stocklist where book_id = ? <br> select * from catalogist where ? like %?% select * from catalogist where ? = ? <br> select s.book_id from stocklist s join cataloglist c on s.isbn = c.isbn where c.title = ? <br> select s.book_id from stocklist s join cataloglist c on s.isbn = c.isbn where c.title = ? |stocklist<br>catalogist |資料IDからISBNを検索するためのDAO
POST/GET|action=rentsearch |BookServlet||BookDAO  getRentedBookIdTitlebyMember(int member_id)|select r.book_id, c.title from rentlist r join stocklist s on r.book_id = s.book_id join cataloglist c on s.isbn = c.isbn where r.return_deadline is null and r.member_id = ?|rentlist /stocklist|現在、貸出中の資料を表示
POST/GET|action=overdue |BookServlet||BookDAO updateRemarksDelay() <br> BookDAO  getRemarksAsOverdue <br> BookDAO getInfoByBookId(int book_id) | update rentlist set remarks = "延滞" where current_date - return_deadline > 0 and return_date is null <br> select * from rentlist where remarks = "延滞" <br> select * from stocklist s join cataloglist c on s.isbn = c.isbn where s.id = ? |rentlist |備考欄に延滞と記録する <br> 備考欄に延滞と記録する <在庫台帳と資料目録を結合して資料IDに当てはまる内容を表示>
POST/GET|action=booksearch |BookServlet||BookDAO  getInfoByIsbn(String isbn) |select * from stocklist where isbn = ?|stocklist |在庫台帳と資料目録を結合して資料IDに当てはまる内容を表示

