# リクエストマッピング

|メソッド|パラメーター|サーブレット|サービス|DAO|SQL|テーブル|内容|
| --- | --- | --- | --- | --- | --- | --- | --- |
POST/GET|action=login|MenuServlet||MenuDAO findName(String id)|SELECT staff_name FROM adminlist WHERE staff_id = ?| adminlist|名前の取得
POST/GET|action=|MenuServlet||MenuDAO findPass(String id)|SELECT staff_password FROM adminlist WHERE Staff_id = ?|adminlist|職員パスワードの取得
POST/GET|action=|MemberServlet||MemberDAO searchLastIdName()|select member_id, member_name from memberlist order by member_id desc limit 1|memberlist|登録された人のIDと名前を取得
POST/GET|action=search|MemberServlet||MemberDAO searchMember(int member_id)|Select * from memberlist where member_id = ?|memberlist|全ての会員情報を取得
POST/GET|action=regist|MemberServlet||MemberDAO addMember(String member_name, String member_address, String member_tel, String member_mail, String member_birth, String current_date)|insert into memberlist(member_name, member_address, member_tel, member_mail, member_birth, member_registdate) values(?, ?, ?, ?, ?, ?)|memberlist|MemberListテーブルに登録
POST/GET|action=update|MemberServlet||MemberDAO updateMember(int member_id, String member_name, String member_address, String member_tel, String member_mail, String member_birth)|update memberlist set member_name = ?, member_address = ?, member_tel = ?, member_mail = ?, member_birth = ? where member_id = ?|memberlist|MemberListテーブルの変更
POST/GET|action=cancel|MemberServlet||MemberDAO cancelMember(int member_id, String current_date)|update memberlist set member_canceldate = ? where member_id = ?|memberlist|退会年月日の情報を追加
POST/GET|action=|BookServlet||BookDAO　findPublishDate(String isbn)|SELECT publish_date FROM cataloglist WHERE  isbn = ?|cataloglist|ISBN番号に属する資料の出版日を取得
POST/GET|action=|BookServlet||BookDAO findinfo(int member_id)|SELECT member_id,book_id,rent_date,return_deadline FROM rentlist WHERE member_id = ?|rentlist|指定された会員番号に属する貸出中の資料の貸出履歴を取得
POST/GET|action=|BookServlet||BookDAO calDelay()|SELECT CURRENT_DATE - return_deadline AS days_diff FROM rentlist|rentlist|指定された会員番号に属する貸出中の資料の貸出履歴を取得
POST/GET|action=|BookServlet||BookDAO findBookId(String isbn)|SELECT book_id FROM stocklist WHERE isbn = ?|stocklist|指定されたISBN番号に属する資料のIDを取得
POST/GET|action=|BookServlet||BookDAO findRentBook(int book_id)|SELECT book_id FROM rentlist WHERE book_id = ? AND return_date is null|rentlist|指定された資料IDの貸出中の資料情報を取得
POST/GET|action=|BookServlet||BookDAO addStock(String isbn,date arrival_date)|insert into stocklist(isbn,arrival_date) values(?,?)|stocklist|資料を在庫台帳に新規登録
POST/GET|action=|BookServlet||BookDAO addCatalog(String isbn)|insert into cataloglist(isbn,title,category_code,writer,publisher,publish_date) values(?,?,?,?,?,?)|cataloglist|資料を在庫台帳に新規登録
POST/GET|action=|BookServlet||BookDAO findDiscard(int book_id)|select * from stocklist where book_id = ?|stocklist|資料IDで資料情報を検索
POST/GET|action=|BookServlet||BookDAO discardBook(date discard_date,String remarks)|insert into stocklist(discard_date,remarks) values(?,?)|stocklist|紛失した資料を廃棄する日付を廃棄年月日として記録
POST/GET|action=|BookServlet||BookDAO findMember(String member_id)|select * from rentlist where member_id = ? and return_date is null|rentlist|会員の貸出状況を確認
POST/GET|action=|BookServlet||BookDAO returnBook(String member_id, String book_id, String current_date)|update rentlist set rent_date = ? where member_id = ? and book_id = ?|rentlist|会員が資料を返却したらテーブルにその日付を登録
POST/GET|action=|BookServlet||BookDAO rentBook(String member_id, String book_id, String current_date String return_deadline)|insert into rentlist(member_id, book_id, rent_date, return_deadline) values(?, ?, ?, ?)<br>select * from rentlist where member_id = ? and book_id = ? and rent_date = ?|rentlist|会員が資料を返却したらテーブルにその日付を登録