# リクエストマッピング

|メソッド|パラメーター|サーブレット|サービス|DAO|SQL|テーブル|内容|
| --- | --- | --- | --- | --- | --- | --- | --- |
POST/GET|action=nulll or “”|MenuServlet||MenuDAO findName(String id)|SELECT staff_name FROM adminlist WHERE staff_id = ?| adminlist|名前の取得
POST/GET|action=add<br>name<br>price|MenuServlet||MenuDAO findPass(String id)|SELECT staff_password FROM adminlist WHERE Staff_id = ?|adminlist|職員パスワードの取得
POST/GET||MemberServlet||MemberDAO searchLastIdName()|select member_id, member_name from memberlist order by member_id desc limit 1|memberlist|登録された人のIDと名前を取得
POST/GET||MemberServlet||MemberDAO searchMember(int member_id)|Select * from memberlist where member_id = ?|memberlist|全ての会員情報を取得
POST/GET||MemberServlet||MemberDAO addMember(String member_name, String member_address, String member_tel, String member_mail, String member_birth, String current_date)|insert into memberlist(member_name, member_address, member_tel, member_mail, member_birth, member_registdate) values(?, ?, ?, ?, ?, ?)|memberlist|MemberListテーブルに登録
POST/GET||MemberServlet||MemberDAO updateMember(int member_id, String member_name, String member_address, String member_tel, String member_mail, String member_birth)|update memberlist set member_name = ?, member_address = ?, member_tel = ?, member_mail = ?, member_birth = ? where member_id = ?|memberlist|MemberListテーブルの変更
POST/GET||MemberServlet||MemberDAO cancelMember(int member_id, String current_date)|update memberlist set member_canceldate = ? where member_id = ?|memberlist|退会年月日の情報を追加